# dependencies
node_modules/
# bun.lock is the repo lockfile (CI installs with --frozen-lockfile);
# package-lock.json appears when npm is used locally — never commit it
package-lock.json

# build artifacts
.next/
out/
dist/
dist-ssr/
*.tsbuildinfo

# sandbox / local-only
dev.log
server.log
tool-results/
.zscripts/
db/
examples/
skills/
upload/
download/
worklog.md
.env
.env.local
*.local

# logs & editor
logs/
*.log
npm-debug.log*
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.sw?
.vercel
scripts/ghsecret/
.deploy-stage/

# ad-film pipeline local artifacts (QA frames, render intermediates —
# never ship these to the repo; final deliverables live in docs/pdf/ and
# the GitHub release)
ad_build/
scripts/keja-docs/doc7_body.pdf
scripts/keja-docs/doc7_cover.html
scripts/keja-docs/doc7_cover.pdf
