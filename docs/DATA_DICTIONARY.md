# Data Dictionary — generated 2026-08-31

> AUTO-GENERATED by `scripts/generate-data-dictionary.mjs` — do not edit by hand.
> Run `node scripts/generate-data-dictionary.mjs` after changing any dataset.

Purpose: every dataset in this demo is documented with its fields so future
contributors (and the Phase-2 backend migration) know exactly what each store
holds. Derived from the TypeScript source of truth.

## `src/data/properties.ts`

Marketplace listing inventory (seed stock).

- **PropertyType** = `'apartment' | 'villa' | 'townhouse' | 'bungalow' | 'land' | 'commercial'`
- **Purpose** = `'buy' | 'rent' | 'invest'`
- **Availability** = `'available' | 'reserved' | 'sold'`
- **TitleStatus** = `'verified' | 'pending' | 'flagged'`

### `Verification`

- titleCheck: TitleStatus;
- ardhisasaMatch: boolean;
- photosVerified: boolean;
- duplicateCheck: 'clean' | 'similar-found' | 'flagged';
- listingVelocity: 'normal' | 'high' | 'suspicious';
- lastChecked: string;

### `TrustSignal`

- label: string;
- status: 'pass' | 'warn' | 'fail';
- detail: string;

### `Property`

- id: string;
- title: string;
- type: PropertyType;
- purpose: Purpose[];
- area: string;
- county: string;
- price: number;
- rentEstimate?: number;
- bedrooms?: number;
- bathrooms?: number;
- sizeSqm: number;
- amenities: string[];
- images: string[];
- description: string;
- agency: string;
- agent: { name: string; phone: string };
- trustScore: number;
- verification: Verification;
- trustSignals: TrustSignal[];
- availability: Availability;
- completionDate?: string;
- paymentPlan?: string;
- grossYieldEstimate?: number;
- appreciationForecast?: number;
- listedAt: string;
- views: number;
- offPlan?: boolean;
- furnished?: boolean;
- highlights: string[];

## `src/data/claims.ts`

Capability claims register (the truth layer).

- **ClaimStatus** = `'live' | 'simulated' | 'partner-dependent' | 'planned'`

### `CapabilityClaim`

- id: string;
- claim: string;
- status: ClaimStatus;
- surface: string;
- evidence: string;
- lastReviewed: string;
- pathToLive?: string;

## `src/data/tokenize.ts`

Tokenization demo assets, ledger and portfolio.

- **TokenizeStatus** = `'LIVE' | 'FUNDING' | 'FUNDED' | 'UPCOMING'`
- **PropertyType** = `'OFFICE' | 'RESIDENTIAL' | 'RETAIL' | 'MIXED_USE' | 'LOGISTICS'`
- **DistributionFreq** = `'MONTHLY' | 'QUARTERLY'`

### `TokenizedProperty`

- id: string;
- slug: string;
- title: string;
- tagline: string;
- description: string;
- location: string;
- city: string;
- country: string;
- propertyType: PropertyType;
- imageUrl: string;
- totalValueUsd: number;
- totalTokens: number;
- tokensSold: number;
- tokenPriceUsd: number;
- annualNetIncomeUsd: number;
- distributionFreq: DistributionFreq;
- legalStructure: string;
- jurisdiction: string;
- tokenSymbol: string;
- contractAddress: string;
- status: TokenizeStatus;
- minTokens: number;
- appreciationPct: number;
- occupancyPct: number;
- managementFeePct: number;
- highlights: string[];
- distributions: PropertyDistribution[];
- investorCount: number;
- custom?: boolean;

### `PropertyDistribution`

- id: string;
- periodLabel: string;
- payDate: string;
- amountUsd: number;
- perTokenUsd: number;

### `Investor`

- id: string;
- email: string;
- fullName: string;
- phone: string;
- country: string;
- kycStatus: 'VERIFIED';
- walletAddress: string;
- demo?: boolean;

### `Investment`

- id: string;
- propertyId: string;
- tokenAmount: number;
- pricePerTokenUsd: number;
- totalCostUsd: number;
- txHash: string;
- blockNumber: number;
- createdAt: string;

### `LedgerTx`

- txHash: string;
- blockNumber: number;
- symbol: string;
- title: string;
- tokens: number;
- totalCostUsd: number;
- timestamp: string;
- type: 'PURCHASE' | 'ISSUANCE' | 'SALE';

### `ReceivedDistribution`

- id: string;
- propertyId: string;
- symbol: string;
- title: string;
- period: string;
- payDate: string;
- tokens: number;
- perTokenUsd: number;
- amountUsd: number;

## `src/data/articles.ts`

Insights/editorial content.

### `ArticleBlock`

- h?: string;
- p?: string;
- list?: string[];
- quote?: string;

### `Article`

- slug: string;
- title: string;
- excerpt: string;
- category: 'Buying Guide' | 'Investing' | 'Diaspora' | 'Trust & Safety' | 'Market Notes';
- minutes: number;
- date: string;
- author: string;
- blocks: ArticleBlock[];

## `src/data/auto-listings.json`

Auto-Pilot ingested listings (machine-screened).

- **version** — `number`
- **generatedAt** — `string`
- **runs** — `array (6 items)`
- **listings** — `array (33 items)`
- **pending** — `array (0 items)`
- **seenSignatures** — `array (10 items)`
- **rejected** — `array (0 items)`

First listing record fields:

- **id** — `string`
- **title** — `string`
- **type** — `string`
- **purpose** — `object`
- **area** — `string`
- **county** — `string`
- **price** — `number` (e.g. 58500000)
- **rentEstimate** — `number` (e.g. 283000)
- **grossYieldEstimate** — `number` (e.g. 5.8)
- **bedrooms** — `number` (e.g. 5)
- **bathrooms** — `number` (e.g. 4)
- **sizeSqm** — `number` (e.g. 380)
- **amenities** — `object`
- **images** — `object`
- **description** — `string`
- **agency** — `string`
- **agent** — `object`
- **availability** — `string`
- **listedAt** — `string`
- **appreciationForecast** — `number` (e.g. 7.5)
- **offPlan** — `boolean`
- **furnished** — `boolean`
- **highlights** — `object`
- **views** — `number` (e.g. 0)
- **auto** — `object`
