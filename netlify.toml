[build]
  # Static export of the client-side SPA (hash routing) — served whole from
  # the CDN. keja.app is the canonical home.
  # NOTE: production deploys are API-zip deploys from GitHub Actions, which
  # do NOT read this file — the same rules ship inside out/ as
  # public/_redirects + public/_headers. Keep both sides in sync.
  command = "NEXT_STATIC=1 npx next build && node scripts/sw-version.mjs"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"

# SPA entry: every path resolves to the app shell; unknown hash routes show
# the in-app 404 view.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"

[[headers]]
  for = "/icons/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"

[[headers]]
  for = "/splash/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"

[[headers]]
  for = "/screenshots/*"
  [headers.values]
    Cache-Control = "public, max-age=604800"

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(self)"
